<UserControl x:Class="ProjectManager.Views.AuthorizationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectManager"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Background="White">

        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Style="{StaticResource SBorder}">
                <StackPanel Margin="91,20,91,20">
                    <TextBlock Margin="0 164 0 39 " Style="{StaticResource STBLOCK}">Вход</TextBlock>
                    <local:PlaceTextBox Margin="0 0 0 61" Width="367" PlaceText ="Логин" Text="{Binding GetLogin}"></local:PlaceTextBox>
                    <local:PlaceTextBox Margin="0 0 0 61" Width="367" PlaceText ="Пароль" Text="{Binding GetPass}"></local:PlaceTextBox>
                    <Button Style="{StaticResource SButton}" Height="62" Width="235"  Command="{Binding AutoCommand}">Войти</Button>
                    <Label Margin="0 0 0 91"></Label>
                </StackPanel>
            </Border>
        </StackPanel>
        
    </Grid>
</UserControl>
