<UserControl x:Class="ProjectManager.Views.AuthorizationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock>Вход</TextBlock>
            <GroupBox Header="Логин">
                <TextBox  Width="150" Text="{Binding GetLogin}"></TextBox>
            </GroupBox>
            <GroupBox Header="Пароль">
                <TextBox  Width="150" Text="{Binding GetPass}"></TextBox>
            </GroupBox>
            <Button Command="{Binding }">Войти</Button>
        </StackPanel>
    </Grid>
</UserControl>
